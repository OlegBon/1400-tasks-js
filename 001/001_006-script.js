let pi = Math.PI; // Отримуємо число π
let outputDiv = document.querySelector(".output");

outputDiv.textContent = pi.toFixed(3); // Форматуємо число до трьох знаків після коми і виводимо його на екран
