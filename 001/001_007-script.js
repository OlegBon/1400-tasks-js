let e = Math.E; // Отримуємо число e (основу натурального логарифма)
let outputDiv = document.querySelector(".output");

outputDiv.textContent = e.toFixed(1); // Форматуємо число до одного знака після коми і виводимо його на екран
